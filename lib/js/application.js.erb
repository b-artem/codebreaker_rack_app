$("#guess-form").submit(function(){
  $("#guess-form").hide();
  // <% if !Validator.code_valid?(guess) %>
  //   alert("Code must contain 4 numbers from 1 to 6. Please try again");
    // pattern = /^[1-6]{4}$/
    // var result = pattern.test(guess);
    // true if input =~ /^[1-6]{4}$/
});
