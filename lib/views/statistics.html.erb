<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Codebreaker Rack App</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="stylesheets/application.css">
  </head>
  <body>
    <div class='container'>
      <section id="buttons">
        <form id="back-btn" class="form-inline" method="post" action="/">
          <button id="submit-btn" type="submit" class="btn btn-primary">Home</button>
        </form>
      </section>

      <section id="statistics">
        <% if stats.empty? %>
          No statistics yet. Play at least one game and save result, please.
        <% end %>
        <% stats.each do |time, result| %>
          Name: <%= result[:name] %><br>
          Score: <%= result[:score] %><br>
          Attempts: <%= result[:attempts] %><br>
          Time: <%= time %><br><br>
        <% end %>
      </section>
    </div>
  </body>
</html>
